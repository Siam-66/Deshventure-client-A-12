import { Link } from "react-router-dom";

const Dashboards = () => {
    return (
        <div>
            <Link to="/addPackage">Add Package</Link>
        </div>
    );
};

export default Dashboards;